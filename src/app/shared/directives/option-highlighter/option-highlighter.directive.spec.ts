import { OptionHighlighterDirective } from './option-highlighter.directive';

describe('OptionHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
